<% flash.each do |type, msg| %>
<div class="alert alert-success alert-dismissible fade show <%= type %>"
     role="alert">
    <%= msg %>
    <button type="button"
            class="btn-close"
            data-bs-dismiss="alert"
            aria-label="Close"></button>
</div>
<% end %>
